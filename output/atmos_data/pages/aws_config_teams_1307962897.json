{
  "title": "aws-config-teams",
  "content": "[profile some-role]\nsource_profile = acme-identity  # â Must match exactly\nbash",
  "code_samples": [
    {
      "code": "### Issue: Credentials stored in `/etc/xdg_config_home` instead of `~/.config`\n\n**Cause**: Geodesic's system-wide XDG environment variables are overriding Atmos defaults.\n\n**Solution**: Set `ATMOS_XDG_CONFIG_HOME` to override Geodesic's system paths:",
      "language": "unknown"
    }
  ],
  "headings": [
    {
      "level": "h3",
      "text": "Issue: Credentials stored in `/etc/xdg_config_home` instead of `~/.config`",
      "id": "issue:-credentials-stored-in-`/etc/xdg_config_home`-instead-of-`~/.config`"
    }
  ],
  "url": "llms-txt#aws-config-teams",
  "links": []
}